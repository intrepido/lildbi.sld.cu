{
   "_id": "_design/solr",
   "language": "javascript",
   "views": {
       "by_id": {
           "map": "function(doc) {emit(doc._id, doc);}"
       }
   },
   "lists": {
       "toSolrXml": "function element(name, row){   for(field in row) {  if (typeof(row[field])=='object') {element(field, row[field])} else {if(isNaN(field)){send('<field name=\"' + field + '\">');  send((''+row[field]).replace(/&/g,'&amp;').replace(/</g,'&lt;'));  send('</field>\\n');}else{send('<field name=\"' + name + '\">');  send((''+row[field]).replace(/&/g,'&amp;').replace(/</g,'&lt;'));  send('</field>\\n');}}  }   };        function(head, req) {    provides('xml', function() {    send('<add>\\n');    while (row=getRow()) {   send('<doc>\\n'); element('doc', row['value']); send('</doc>\\n');   }    send('</add>');   }); }"
   }
}